# ── CodeQuest API — Python 3.13 compatible ───────────────────────────────────
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.1

# ASGI adapter for Vercel serverless (AWS Lambda-compatible)
mangum==0.17.0

# Data validation & serialisation (Pydantic v2)
pydantic==2.8.2

# ASGI server for local development
uvicorn[standard]==0.30.3

# In-memory caching with TTL support
cachetools==5.3.3

# HTTP utilities (pulled in by fastapi, pinned for reproducibility)
httpx==0.27.0
anyio==4.4.0
starlette==0.37.2

# Type annotations back-compat (stdlib in 3.11+, kept for tooling)
typing_extensions==4.12.2
